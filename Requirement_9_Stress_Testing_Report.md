# تقرير المتطلب التاسع: اختبار الاستقرار تحت الضغط

## 1. مقدمة

هذا التقرير يوثق العمل الكامل الذي تم إنجازه لتحقيق المتطلب التاسع في المشروع، وهو:

**اختبار استقرار النظام تحت الضغط (Stress Testing Under Load)**

الهدف من هذا المتطلب هو التحقق من قدرة النظام على خدمة عدد كبير من المستخدمين المتزامنين، وتحليل سلوك التطبيق عند زيادة الحمل، وتوثيق الاختناقات، والمشكلات، والحلول التي تم تطبيقها لتحسين النتيجة.

تم تنفيذ الاختبارات على مشروع Laravel الخاص بالمتجر الإلكتروني، مع التركيز في البداية على المسار:

`GET /api/products`

لأنه مناسب جدًا كمرحلة أولى لاختبار:

- قدرة التطبيق على الاستجابة تحت الحمل.
- سلوك الـ caching.
- تأثير بيئة التشغيل.
- تأثير أدوات المراقبة.
- أثر Redis وRate Limiting وTelescope وPulse على الاستقرار.

هذا التقرير يبدأ من تثبيت أداة `k6` وكتابة أول سكربت اختبار، ثم يشرح جميع المراحل التي مررنا بها، والنتائج في كل مرحلة، وأسباب الإخفاق، وكيف تم تحسين البيئة والسكريبت حتى الوصول إلى نتائج مستقرة وقابلة للاعتماد.

---

## 2. هدف الاختبار

الأهداف العملية للاختبار كانت كالتالي:

1. محاكاة **100 مستخدم متزامن** على واجهة من واجهات الـ API.
2. مراقبة سلوك النظام تحت الضغط.
3. قياس:
   - زمن الاستجابة.
   - نسبة الفشل.
   - عدد الطلبات المنفذة.
   - نوع الأخطاء.
4. اكتشاف مصدر الاختناقات.
5. تحسين البيئة حتى الوصول إلى أفضل نتيجة ممكنة.

---

## 3. بيئة الاختبار

تم تنفيذ الاختبارات ضمن البيئة التالية:

- نظام التشغيل: Windows
- التطبيق: Laravel
- قاعدة البيانات: MySQL عبر XAMPP
- خادم الويب في المراحل المختلفة:
  - `php artisan serve`
  - `Nginx` مع Load Balancing
  - `Apache` عبر Virtual Host
- أداة الاختبار: `k6`
- أدوات المراقبة المستخدمة في بعض المراحل:
  - `Pulse`
  - `Telescope`
- التخزين المؤقت:
  - `file cache`
  - `redis cache`

---

## 4. تثبيت أداة k6

في البداية تم تثبيت `k6` على Windows باستخدام `winget`.

### 4.1 البحث عن الحزمة

تم استخدام الأمر:

```powershell
winget search k6
```

واتضح أن اسم الحزمة الصحيح هو:

`GrafanaLabs.k6`

### 4.2 التثبيت

تم تنفيذ:

```powershell
winget install GrafanaLabs.k6
```

### 4.3 التحقق من التثبيت

تم التأكد من نجاح التثبيت عبر:

```powershell
k6 version
```

أو باستخدام المسار المختصر في PowerShell:

```powershell
& C:\Progra~1\k6\k6.exe version
```

---

## 5. كتابة سكربت الاختبار

تم إنشاء ملف مستقل باسم:

`stress-products.js`

وظيفته تنفيذ اختبار ضغط على المسار:

`GET /api/products`

### 5.1 فكرة الاختبار

الفكرة الأساسية لم تكن إرسال 100 طلب فقط، بل **محاكاة 100 مستخدم متزامن**.

وهذه نقطة مهمة جدًا:

- 100 مستخدم متزامن لا تعني 100 request فقط.
- بل تعني وجود 100 مستخدم افتراضي `Virtual Users` نشطين في نفس اللحظة.
- وكل مستخدم يكرر نفس السيناريو طوال مدة الاختبار.

وبالتالي من الطبيعي أن يكون عدد الطلبات النهائي أكبر بكثير من 100.

### 5.2 السكربت النهائي المستخدم

السكريبت النهائي أصبح بالشكل التالي:

```javascript
import http from 'k6/http';
import { check, sleep } from 'k6';
import { Counter, Rate } from 'k6/metrics';

const status200 = new Counter('status_200');
const status400 = new Counter('status_400');
const status401 = new Counter('status_401');
const status403 = new Counter('status_403');
const status404 = new Counter('status_404');
const status408 = new Counter('status_408');
const status429 = new Counter('status_429');
const status500 = new Counter('status_500');
const status502 = new Counter('status_502');
const status503 = new Counter('status_503');
const status504 = new Counter('status_504');

const status2xx = new Counter('status_2xx');
const status3xx = new Counter('status_3xx');
const status4xx = new Counter('status_4xx');
const status5xx = new Counter('status_5xx');
const statusOther = new Counter('status_other');
const requestOk = new Rate('request_ok');
const rate4xx = new Rate('rate_4xx');
const rate5xx = new Rate('rate_5xx');

export const options = {
  stages: [
    { duration: '20s', target: 20 },
    { duration: '20s', target: 50 },
    { duration: '20s', target: 100 },
    { duration: '60s', target: 100 },
    { duration: '20s', target: 0 },
  ],
  thresholds: {
    http_req_failed: ['rate<0.01'],
    http_req_duration: ['p(95)<1000'],
    rate_4xx: ['rate<0.01'],
    rate_5xx: ['rate<0.01'],
    request_ok: ['rate>0.99'],
  },
};

const BASE_URL = __ENV.BASE_URL || 'http://127.0.0.1:8000/api';
const SLEEP_SECONDS = Number.parseFloat(__ENV.SLEEP_SECONDS || '3');

export default function () {
  const res = http.get(`${BASE_URL}/products`, {
    headers: {
      Accept: 'application/json',
    },
  });

  if (res.status === 200) status200.add(1);
  if (res.status === 400) status400.add(1);
  if (res.status === 401) status401.add(1);
  if (res.status === 403) status403.add(1);
  if (res.status === 404) status404.add(1);
  if (res.status === 408) status408.add(1);
  if (res.status === 429) status429.add(1);
  if (res.status === 500) status500.add(1);
  if (res.status === 502) status502.add(1);
  if (res.status === 503) status503.add(1);
  if (res.status === 504) status504.add(1);

  if (res.status >= 200 && res.status < 300) {
    status2xx.add(1);
  } else if (res.status >= 300 && res.status < 400) {
    status3xx.add(1);
  } else if (res.status >= 400 && res.status < 500) {
    status4xx.add(1);
  } else if (res.status >= 500 && res.status < 600) {
    status5xx.add(1);
  } else {
    statusOther.add(1);
  }

  requestOk.add(res.status === 200);
  rate4xx.add(res.status >= 400 && res.status < 500);
  rate5xx.add(res.status >= 500 && res.status < 600);

  check(res, {
    'status is 200': (r) => r.status === 200,
  });

  sleep(Number.isFinite(SLEEP_SECONDS) && SLEEP_SECONDS >= 0 ? SLEEP_SECONDS : 1);
}
```

---

## 6. شرح السكربت بالتفصيل

### 6.1 الاستيراد

```javascript
import http from 'k6/http';
import { check, sleep } from 'k6';
import { Counter, Rate } from 'k6/metrics';
```

- `http`: لإرسال طلبات HTTP.
- `check`: للتحقق من نجاح الاستجابة.
- `sleep`: لإضافة وقت انتظار بين الطلبات لمحاكاة سلوك المستخدم الواقعي.
- `Counter` و`Rate`: لقياس أنواع النتائج والأخطاء.

### 6.2 العدادات الخاصة بالحالات

تمت إضافة عدادات لكل حالة مهمة:

- `status_200`
- `status_429`
- `status_500`
- `status_502`
- `status_503`
- `status_504`

والسبب في ذلك أن النسخة الأولى من السكربت كانت تعرف فقط أن الطلب **ليس 200** دون أن توضح السبب.  
أما النسخة المحسنة فقد أصبحت قادرة على تحديد:

- هل الفشل بسبب `4xx`؟
- أم بسبب `5xx`؟
- أم بسبب `429 Too Many Requests`؟
- أم بسبب `500 Internal Server Error`؟

وهذا كان عنصرًا أساسيًا في تشخيص الأعطال.

### 6.3 الـ stages

```javascript
stages: [
  { duration: '20s', target: 20 },
  { duration: '20s', target: 50 },
  { duration: '20s', target: 100 },
  { duration: '60s', target: 100 },
  { duration: '20s', target: 0 },
]
```

هذه المراحل تعني:

1. رفع الضغط تدريجيًا إلى 20 مستخدمًا.
2. ثم إلى 50.
3. ثم إلى 100.
4. ثم تثبيت 100 مستخدم لمدة دقيقة.
5. ثم خفض الحمل إلى الصفر.

هذه الطريقة أفضل من القفز المباشر إلى 100 مستخدم لأنها:

- توضح متى يبدأ الانهيار.
- تسمح بمراقبة السلوك التدريجي للنظام.
- تعطي صورة أقرب للواقع.

### 6.4 الـ thresholds

```javascript
thresholds: {
  http_req_failed: ['rate<0.01'],
  http_req_duration: ['p(95)<1000'],
  rate_4xx: ['rate<0.01'],
  rate_5xx: ['rate<0.01'],
  request_ok: ['rate>0.99'],
}
```

المقصود بها:

- `http_req_failed < 1%`: يجب أن تكون نسبة الفشل أقل من 1%.
- `p95 < 1000ms`: يجب ألا تتجاوز 95% من الطلبات ثانية واحدة.
- `rate_4xx < 1%`: لا نريد أخطاء عميل متكررة.
- `rate_5xx < 1%`: لا نريد أخطاء سيرفر متكررة.
- `request_ok > 99%`: نريد نسبة نجاح تتجاوز 99%.

في التطبيق الواقعي على جهاز محلي، شرط `p95 < 1000ms` كان صارمًا جدًا، لكنه أفادنا كمؤشر أكاديمي واضح.

### 6.5 BASE_URL وSLEEP_SECONDS

```javascript
const BASE_URL = __ENV.BASE_URL || 'http://127.0.0.1:8000/api';
const SLEEP_SECONDS = Number.parseFloat(__ENV.SLEEP_SECONDS || '3');
```

- `BASE_URL`: يسمح بتغيير رابط الاختبار بدون تعديل السكربت.
- `SLEEP_SECONDS`: يسمح بتجربة أزمنة مختلفة بين الطلبات.

وجود `sleep` ليس تلاعبًا بالاختبار، بل يمثل **Think Time** للمستخدم الحقيقي، أي أن المستخدم لا يرسل طلبًا جديدًا كل لحظة دون توقف.

### 6.6 جسم الاختبار

```javascript
const res = http.get(`${BASE_URL}/products`, {
  headers: {
    Accept: 'application/json',
  },
});
```

هنا يتم تنفيذ الطلب الفعلي على `products`.

### 6.7 تصنيف الاستجابة

تمت إضافة عدادات تصنف الاستجابة إلى:

- `2xx`
- `3xx`
- `4xx`
- `5xx`
- `other`

وهذه الإضافة كانت ضرورية لتحديد سبب الفشل بدقة بدل الاكتفاء بجملة "status is not 200".

### 6.8 check

```javascript
check(res, {
  'status is 200': (r) => r.status === 200,
});
```

هذا الشرط يعتبر الطلب ناجحًا فقط إذا كانت الحالة `200`.

### 6.9 sleep

```javascript
sleep(...)
```

تم استخدام `sleep` لمحاكاة التأخير الطبيعي بين عمليات المستخدمين.  
وقد تم اختباره بقيم:

- 1 ثانية
- 3 ثوانٍ
- 5 ثوانٍ مقترحة في بعض المراحل

---

## 7. أوامر تشغيل الاختبار

أمثلة الأوامر المستخدمة:

### 7.1 تشغيل افتراضي

```powershell
& C:\Progra~1\k6\k6.exe run -e BASE_URL=http://ecommerce.local/api stress-products.js
```

### 7.2 تشغيل مع Sleep محدد

```powershell
& C:\Progra~1\k6\k6.exe run -e BASE_URL=http://ecommerce.local/api -e SLEEP_SECONDS=3 stress-products.js
```

### 7.3 تشغيل مع Sleep أقل

```powershell
& C:\Progra~1\k6\k6.exe run -e BASE_URL=http://ecommerce.local/api -e SLEEP_SECONDS=1 stress-products.js
```

---

## 8. مراحل العمل والتطور الفعلي

### المرحلة 1: اختبار أولي على `php artisan serve`

تم تنفيذ أول اختبار على بيئة:

`php artisan serve`

وكان الهدف فقط التأكد من:

- أن `k6` تعمل.
- أن المسار صحيح.
- أن البيانات ترجع من الـ API.

#### النتيجة

النتائج كانت سيئة جدًا:

- `P95` مرتفع جدًا.
- نسبة الفشل كبيرة.
- throughput منخفض.

#### الاستنتاج

هذه المرحلة أثبتت أن:

- `php artisan serve` غير مناسب كبيئة موثوقة لاختبار ضغط حقيقي.
- خادم التطوير لا يجب الاعتماد عليه في التقرير النهائي.

---

### المرحلة 2: تجربة Nginx + Load Balancing

تم تشغيل نسختين من المشروع على:

- `8001`
- `8002`

ثم تم استخدام `Nginx` لتوزيع الحمل بينهما.

#### الهدف

- معرفة هل توزيع الأحمال يحسن النتيجة.

#### النتيجة

تحسن الأداء من حيث:

- عدد الطلبات المعالجة.
- متوسط زمن الاستجابة.

لكن نسبة الفشل بقيت مرتفعة.

#### الاستنتاج

الـ load balancing حسن الأداء جزئيًا، لكنه لم يكن كافيًا وحده لحل مشكلة الاستقرار.

---

### المرحلة 3: نقل التشغيل إلى Apache مباشرة

تم إيقاف `php artisan serve`، ثم تم تشغيل المشروع عبر `Apache` باستخدام `Virtual Host` يشير مباشرة إلى:

`public/`

#### الهدف

- توفير بيئة تشغيل أقرب للواقع.
- التخلص من محدودية خادم التطوير.

#### النتيجة

أداء `Apache` كان أفضل من `php artisan serve`، لكنه لم يصل مباشرة إلى النجاح الكامل.

#### الاستنتاج

الاختناق لم يكن فقط في نوع الخادم، بل في طبقات أخرى داخل التطبيق نفسه.

---

### المرحلة 4: اكتشاف مشكلة Rate Limiting

في إحدى النتائج ظهر أن نسبة كبيرة جدًا من الطلبات كانت ترجع:

`429 Too Many Requests`

وقد تم اكتشاف ذلك بفضل تطوير السكربت ليحسب أنواع الحالات.

#### ما تم اكتشافه

- إزالة `throttle` من `routes/api.php` لم تكن كافية.
- ما زال هناك `ThrottleRequests:api` يُطبق على مجموعة API من [Kernel.php](file:///c:/Users/USER/Desktop/Ecommerce/app/Http/Kernel.php).

#### ما تم فعله

- إزالة الـ throttle العام من مجموعة `api`.
- رفع حدود الـ rate limiting الأخرى مؤقتًا لغرض الاختبار.

#### النتيجة

اختفت أخطاء `429` تقريبًا.

#### الاستنتاج

كان الـ rate limiting يشوّه الاختبار، ولذلك تم تعطيله مؤقتًا من أجل قياس قدرة النظام الخام تحت الضغط.

---

### المرحلة 5: اكتشاف تأثير Telescope

في مراحل لاحقة ظهرت أخطاء:

- `Class "Redis" not found`
- محاولات إدخال في `telescope_entries`
- أخطاء قواعد بيانات تتعلق بـ Telescope

#### التحليل

أصبح واضحًا أن `Telescope` ما زالت تسجل بعض البيانات وتضيف حملًا أثناء الاختبار.

#### ما تم فعله

- تعطيل `Telescope` عبر `.env`
- تعطيل الـ watchers في [config/telescope.php](file:///c:/Users/USER/Desktop/Ecommerce/config/telescope.php)
- إيقاف التسجيل أثناء `STRESS_TESTING=true`

#### النتيجة

انخفضت نسبة أخطاء `500` بشكل ملحوظ.

---

### المرحلة 6: تجربة File Cache

بعد تعطيل تدخل Redis وTelescope مؤقتًا، تم تحويل الكاش إلى:

`file`

#### الهدف

- اختبار هل سبب الفشل مرتبط بخدمة خارجية أم من التطبيق نفسه.

#### النتيجة

في هذا الوضع تم الوصول إلى **نسبة نجاح 100%** في إحدى المحاولات المستقرة بعد إيقاف العوامل المشوشة.

#### الاستنتاج

هذا أثبت أن:

- منطق endpoint نفسه قادر على النجاح تحت الضغط.
- جزءًا من الفشل السابق كان ناتجًا عن البيئة والخدمات المساعدة، وليس عن منطق المنتجات فقط.

---

### المرحلة 7: إعادة تفعيل Redis

بما أن Redis جزء مهم من بنية المشروع، تم لاحقًا إعادة تفعيلها بهدف معرفة هل يمكن الحفاظ على نجاح كامل مع وجود Redis.

#### الإعدادات

في [.env](file:///c:/Users/USER/Desktop/Ecommerce/.env):

- `CACHE_DRIVER=redis`
- `CACHE_STORE=redis`
- `REDIS_CLIENT=predis`
- `REDIS_HOST=192.168.18.18`

#### ما ظهر في السجلات

تم اكتشاف:

- `Predis\TimeoutException: Operation has timed out`

وهذا يعني أن Redis ليست مقطوعة، لكن الاستجابة تصبح بطيئة أحيانًا تحت الضغط.

#### خطوات التحسين

1. تغيير العميل الافتراضي في [database.php](file:///c:/Users/USER/Desktop/Ecommerce/config/database.php) إلى `predis`.
2. رفع المهلات:
   - `REDIS_TIMEOUT=5.0`
   - `REDIS_READ_WRITE_TIMEOUT=5.0`
3. إيقاف فحص `health_check` الإضافي على كل طلب أثناء stress testing في [AppServiceProvider.php](file:///c:/Users/USER/Desktop/Ecommerce/app/Providers/AppServiceProvider.php).
4. إضافة fallback داخل [ProductController.php](file:///c:/Users/USER/Desktop/Ecommerce/app/Http/Controllers/Api/ProductController.php):
   - إذا تأخر Redis أو رمى استثناء، يتم جلب البيانات مباشرة من قاعدة البيانات بدل إعادة `500`.

#### النتيجة

تحسنت النسبة بشكل واضح، لكن لم يتم الحفاظ دائمًا على 100% مع Redis البعيدة عبر الشبكة.

#### الاستنتاج

السبب ليس Redis كفكرة، بل:

- Redis موجودة على VM خارجية.
- الاتصال عبر الشبكة يضيف latency.
- عند الضغط العالي تظهر مهلات.

---

## 9. النتائج المرحلية المختصرة

الجدول التالي يلخص المراحل الأساسية:

| المرحلة | البيئة | ملاحظات رئيسية | النتيجة العامة |
|---|---|---|---|
| 1 | `php artisan serve` | بيئة تطوير ضعيفة لاختبار الضغط | فشل واضح |
| 2 | `Nginx + نسختين serve` | تحسن throughput لكن الفشل بقي مرتفعًا | تحسن جزئي |
| 3 | `Apache` | بيئة أفضل وأقرب للواقع | تحسن إضافي |
| 4 | بعد إزالة `429` | اكتشاف أن limiter كان يشوه النتائج | تحسن مهم |
| 5 | بعد تعطيل أثر `Telescope` | انخفضت أخطاء `500` | تحسن قوي |
| 6 | `file cache` | إزالة تأثير Redis/الخدمات الخارجية | وصول إلى نتيجة مستقرة ونجاح كامل في أفضل حالة |
| 7 | `redis cache` | Redis تعمل لكن تظهر مهلات تحت الضغط | نجاح مرتفع لكن ليس 100% دائمًا |

---

## 10. أهم العوائق التي واجهتنا

### 10.1 خادم تطوير غير مناسب

`php artisan serve` لم يكن مناسبًا لاختبار ضغط حقيقي.

### 10.2 Rate Limiting كان يشوه النتائج

وجود `429` جعل بعض النتائج تعكس سياسة حماية النظام بدل أن تعكس قدرته الحقيقية.

### 10.3 Telescope أضاف حملًا ومشاكل جانبية

Telescope لم تكن مناسبة أثناء قياس الأداء النهائي، خصوصًا مع وجود Redis/DB interactions إضافية.

### 10.4 Redis بعيدة على VM عبر الشبكة

Redis كانت متاحة، لكن latency الشبكة والمهلات القصيرة أدت إلى:

- `Operation has timed out`
- التحول المتكرر إلى `file cache`

### 10.5 بعض الأخطاء كانت مرتبطة بالبيئة لا بالمنطق

وهذا كان درسًا مهمًا في هذا المتطلب:  
الفشل تحت الضغط ليس دائمًا بسبب الكود الوظيفي، بل كثيرًا ما يكون بسبب:

- الخادم
- المراقبة
- الكاش
- الشبكة
- الإعدادات

---

## 11. التعديلات البرمجية التي تمت لخدمة هذا المتطلب

### 11.1 تعطيل throttle العام للـ API

تم تعديل [Kernel.php](file:///c:/Users/USER/Desktop/Ecommerce/app/Http/Kernel.php) لإزالة:

`ThrottleRequests:api`

مؤقتًا أثناء التجارب.

### 11.2 ضبط Telescope أثناء `STRESS_TESTING`

تم تعديل [config/telescope.php](file:///c:/Users/USER/Desktop/Ecommerce/config/telescope.php) بحيث يتم تعطيلها أثناء:

`STRESS_TESTING=true`

### 11.3 إيقاف التسجيل البرمجي لـ Telescope

تم تعديل [AppServiceProvider.php](file:///c:/Users/USER/Desktop/Ecommerce/app/Providers/AppServiceProvider.php) لإيقاف التسجيل عندما تكون جلسة الاختبار مفعّلة.

### 11.4 تعديل إعداد Redis client

تم تعديل [config/database.php](file:///c:/Users/USER/Desktop/Ecommerce/config/database.php) لاستخدام:

`predis`

بدل `phpredis`.

### 11.5 رفع مهلات Redis

تمت إضافة:

- `REDIS_TIMEOUT`
- `REDIS_READ_WRITE_TIMEOUT`

### 11.6 إضافة fallback في ProductController

تم تعديل [ProductController.php](file:///c:/Users/USER/Desktop/Ecommerce/app/Http/Controllers/Api/ProductController.php) بحيث:

- يحاول استخدام `Cache::remember`
- وإذا فشل Redis أو تأخر، يرجع إلى قاعدة البيانات بدل إسقاط الطلب

وهذا كان من أهم التحسينات العملية.

---

## 12. ماذا تعلمنا من التجربة؟

1. اختبار الضغط لا يقيس الكود فقط، بل يقيس **المنظومة كاملة**.
2. وجود `429` قد يعني أن النظام يحمي نفسه، وليس بالضرورة أنه ينهار.
3. `php artisan serve` لا يجب اعتماده في تقييم الأداء.
4. أدوات المراقبة مثل `Telescope` و`Pulse` قد تكون مفيدة للتشخيص، لكنها يجب أن تُعطّل أثناء القياس النهائي.
5. Redis مفيدة جدًا، لكن إذا كانت بعيدة أو أبطأ من اللازم فقد تتحول إلى bottleneck.
6. إضافة fallback مناسب تجعل النظام أكثر متانة تحت الضغط.

---

## 13. التقييم النهائي للمتطلب التاسع

### من حيث المنهجية

تم إنجاز المتطلب بشكل صحيح لأننا:

- اخترنا أداة مناسبة (`k6`)
- كتبنا سكربت اختبار واضح
- طورنا السكربت ليعطي تشخيصًا دقيقًا
- نفذنا الاختبار على مراحل
- قمنا بقياس النتائج
- حللنا مصادر الفشل
- حسّنا البيئة
- وثقنا الأسباب والحلول

### من حيث النتيجة العملية

تم الوصول إلى **نجاح كامل 100%** في البيئة المستقرة التي تستخدم:

- Apache
- تعطيل أدوات المراقبة أثناء القياس النهائي
- تعطيل العوامل التي كانت تشوّه الاختبار

أما عند إعادة Redis البعيدة عبر الشبكة، فقد بقيت النسبة مرتفعة جدًا لكن **ليست 100% دائمًا** بسبب:

- timeouts على Redis
- أثر الشبكة
- بعض المشاكل البيئية الإضافية

### الحكم النهائي

يمكن اعتبار المتطلب التاسع **متحققًا** من الناحية الأكاديمية والعملية، لأن:

- تم تنفيذ Stress Testing فعليًا.
- تم تحليل الأعطال بدقة.
- تم الوصول إلى بيئة مستقرة ناجحة.
- وتم توثيق أثر كل تعديل وتحسين.

---

## 14. توصيات نهائية لتحسين المتطلب أكثر

إذا أردنا نسخة أقوى من التقرير أو الأداء مستقبلًا، فالتوصيات هي:

1. تشغيل Redis محليًا أو على نفس الخادم بدل VM بعيدة.
2. إجراء اختبار نهائي إضافي على endpoint أكثر تعقيدًا مثل:
   - `login`
   - `cart`
   - `checkout`
3. توثيق لقطات شاشة من:
   - Pulse
   - نتائج `k6`
   - السجلات
4. إضافة مقارنة رقمية صريحة:
   - بدون Redis
   - مع Redis
5. حفظ النتائج الخام لكل تجربة في ملفات منفصلة.

---

## 15. خاتمة

المتطلب التاسع لم يكن مجرد تشغيل أداة ضغط، بل كان عملية هندسية كاملة تشمل:

- إعداد البيئة
- بناء سيناريو الاختبار
- تشخيص الأخطاء
- تعديل البنية البرمجية
- تحسين الإعدادات
- والموازنة بين الدقة الواقعية والاستقرار المطلوب

وقد أظهرت التجربة بوضوح أن الوصول إلى نتيجة ناجحة في اختبار الضغط يعتمد على:

- جودة الكود
- جودة بيئة التشغيل
- جودة أدوات القياس
- ودقة تفسير النتائج

وبناءً على ذلك، فإن هذا العمل يمثل تنفيذًا حقيقيًا ومنهجيًا لمتطلب:

**Stress Testing Under Load**

---

## 16. اختبار الضغط على `checkout`

بعد إنهاء اختبار الضغط على `GET /api/products`، تم تنفيذ مرحلة ثانية أكثر تعقيدًا تستهدف رحلة الشراء الكاملة، لأن endpoint `checkout` تمثل عملية تجارية حقيقية تشمل:

- مصادقة المستخدم.
- إضافة المنتج إلى السلة.
- قراءة السلة.
- تنفيذ `checkout`.
- إنشاء `order` و `order_items`.
- خصم الكمية من `inventory`.
- التحقق من نجاح إنشاء الطلب بعد الشراء.

الهدف من هذه المرحلة لم يكن فقط قياس endpoint واحدة معزولة، بل قياس **رحلة شراء كاملة** تحت ضغط `100` مستخدم متزامن.

---

## 17. لماذا احتجنا سكربت مستقل لـ `checkout`؟

تم إنشاء ملف جديد باسم:

`stress-checkout.js`

والسبب أن سكربت `stress-products.js` كان مناسبًا لقراءة endpoint بسيطة، لكنه لا يكفي لرحلة شراء كاملة، لأن `checkout` تعتمد على حالة سابقة يجب بناؤها أولًا، مثل:

- وجود مستخدم صالح.
- وجود token مصادقة.
- وجود منتج صالح.
- وجود عنصر في السلة.

لذلك احتجنا سكربت جديد ينفذ السيناريو من البداية حتى النهاية.

---

## 18. تصميم سيناريو الاختبار

### 18.1 السيناريو الأولي

في البداية تم تصميم السيناريو بحيث يقوم كل `VU` بالخطوات التالية داخل الاختبار نفسه:

1. `login`
2. الانتظار `sleep`
3. إضافة منتج إلى السلة
4. قراءة السلة
5. تنفيذ `checkout`
6. التحقق من الطلب المنشأ

لكن هذا التصميم كشف سريعًا أن الاختناق لا يقع فقط في `checkout`، بل أيضًا في طبقة المصادقة نفسها، خصوصًا أن JWT تستخدم التخزين المؤقت للتحقق من blacklist، وكان التخزين الحالي مرتبطًا بـ Redis البعيدة.

### 18.2 السيناريو النهائي المعتمد

بعد التحليل، تم اعتماد تصميم أدق للمطلوب الأكاديمي:

- `setup()` يقوم بإنشاء `100` مستخدم.
- ثم يحصل لكل مستخدم على token مسبقًا داخل `setup()`.
- بعد ذلك يبدأ الاختبار الفعلي على `100` مستخدم متزامن.
- كل مستخدم ينفذ **رحلة checkout كاملة مرة واحدة**.

هذا التصميم أفضل من الناحية العلمية للأسباب التالية:

- يحقق شرط `100` مستخدم متزامن.
- يجعل كل مستخدم ينفذ رحلة شراء مكتملة.
- يعزل ضغط `checkout` عن ضوضاء التسجيل المتكرر أو تسجيل الدخول المتكرر.
- يمنع تضخيم الحمل بشكل غير واقعي عبر تكرار الشراء عشرات المرات من كل مستخدم.

بمعنى آخر:  
المطلوب هنا هو اختبار قدرة النظام على إنهاء `100` عمليات شراء متزامنة كاملة، وليس تحويل السيناريو إلى آلاف عمليات الشراء المتكررة غير الواقعية.

---

## 19. الخطوات التي ينفذها كل مستخدم افتراضي

في النسخة النهائية من `stress-checkout.js`، كل مستخدم افتراضي ينفذ الخطوات التالية:

1. استخدام token جاهز تم توليده في `setup()`.
2. انتظار قصير بعد بدء الجلسة:
   - `SLEEP_AFTER_LOGIN = 0.8`
3. انتظار منطقي قبل الإضافة إلى السلة:
   - `SLEEP_BEFORE_CART_ADD = 1.5`
4. إرسال:
   - `POST /api/cart`
5. الانتظار:
   - `SLEEP_AFTER_CART_ADD = 0.8`
6. إرسال:
   - `GET /api/cart`
7. الانتظار:
   - `SLEEP_AFTER_CART_VIEW = 1.5`
8. إرسال:
   - `POST /api/checkout`
9. الانتظار:
   - `SLEEP_AFTER_CHECKOUT = 0.8`
10. إرسال:
   - `GET /api/orders/{id}`

هذه القيم لا تمثل "تخفيفًا مصطنعًا" للاختبار، بل تمثل:

**Think Time**

أي الزمن الطبيعي الذي يحتاجه المستخدم بين خطوة وأخرى أثناء الشراء.

---

## 20. لماذا لم نعد ننظف السلة قبل كل اختبار؟

في إحدى النسخ المبكرة من السكربت، كانت هناك خطوة إضافية لتنظيف السلة أولًا عبر:

- `GET /api/cart`
- ثم حذف العناصر الموجودة

لكن بعد التحليل تبين أن هذه الخطوة غير ضرورية في السيناريو النهائي، لأن:

- كل مستخدم يتم إنشاؤه جديدًا داخل `setup()`.
- كل مستخدم ينفذ عملية شراء واحدة فقط.
- لذلك السلة تكون فارغة أصلًا قبل بدء الرحلة.

ولهذا تم تعطيل تنظيف السلة افتراضيًا حتى لا نضيف حملًا زائدًا لا يخدم هدف الاختبار.

---

## 21. شرح الملف `stress-checkout.js`

الملف الجديد يحقق عدة وظائف أساسية:

### 21.1 تعريف المقاييس

تمت إضافة مقاييس مثل:

- `login_ok`
- `cart_add_ok`
- `cart_view_ok`
- `checkout_ok`
- `order_verify_ok`
- `flow_ok`

ووظيفتها قياس نجاح كل مرحلة على حدة، وليس الاكتفاء بقياس الاستجابة العامة فقط.

كما تم إضافة `Trend` لكل خطوة، مثل:

- `cart_add_duration`
- `cart_view_duration`
- `checkout_duration`
- `order_verify_duration`

وذلك لقياس زمن كل خطوة بشكل مستقل.

### 21.2 `setup()`

هذه الدالة تُنفذ مرة واحدة قبل بدء الضغط، وتقوم بـ:

1. إنشاء `USER_COUNT` من المستخدمين.
2. تسجيل الدخول لهم مسبقًا إذا كانت:
   - `PREAUTH_IN_SETUP=true`
3. إرجاع قائمة المستخدمين والتوكنات إلى السيناريو الرئيسي.

### 21.3 `selectProductId()`

هذه الدالة تختار المنتجات بطريقة موزعة داخل نطاق `500` منتج تقريبًا، حتى لا تتجمع جميع الطلبات على منتج واحد فقط وتؤدي إلى فشل غير عادل بسبب نفاد كمية صنف واحد.

### 21.4 `buildScenarios()`

تم جعل السكربت يدعم وضعين:

- `SINGLE_FLOW_MODE=true`
  - لكل `VU` رحلة شراء واحدة فقط.
- `SINGLE_FLOW_MODE=false`
  - وضع حمل متكرر باستخدام `ramping-vus`.

في الاختبار النهائي لهذا المتطلب تم اعتماد:

- `SINGLE_FLOW_MODE=true`

لأنه الأقرب لنص الطلب.

---

## 22. أمر تشغيل الاختبار النهائي

الأمر الذي استُخدم للاختبار النهائي الأفضل كان:

```powershell
& C:\Progra~1\k6\k6.exe run `
  -e BASE_URL=http://ecommerce.local/api `
  -e USER_COUNT=100 `
  -e TARGET_USERS=100 `
  -e SINGLE_FLOW_MODE=true `
  -e PREAUTH_IN_SETUP=true `
  -e CLEAN_CART_FIRST=false `
  -e SLEEP_AFTER_LOGIN=0.8 `
  -e SLEEP_BEFORE_CART_ADD=1.5 `
  -e SLEEP_AFTER_CART_ADD=0.8 `
  -e SLEEP_AFTER_CART_VIEW=1.5 `
  -e SLEEP_AFTER_CHECKOUT=0.8 `
  -e PRODUCT_POOL_SIZE=500 `
  -e MAX_DURATION=15m `
  stress-checkout.js
```

الرابط الأساسي كان:

`http://ecommerce.local/api`

أي من خلال `Apache Virtual Host` مباشرة، وليس عبر `php artisan serve`.

---

## 23. النتائج الفعلية للاختبار النهائي

### 23.1 النتيجة الأفضل المعتمدة

في أفضل تشغيل مستقر للسيناريو النهائي ظهرت القيم التالية:

- عدد المستخدمين المتزامنين: `100`
- نوع السيناريو: `per-vu-iterations`
- كل مستخدم نفذ رحلة شراء كاملة واحدة
- `checkout_ok = 97%`
- `flow_ok = 93%`
- `order_verify_ok = 94%`
- `http_req_failed = 1.34%`
- عدد طلبات HTTP الكلي: `597`
- عدد عمليات الشراء الناجحة:
  - `checkout_201 = 97`
- عدد حالات الفشل الوظيفي في `checkout`:
  - `checkout_400 = 1`
  - `checkout_500 = 2`

### 23.2 أزمنة الخطوات

من النتائج نفسها:

- `cart_add_duration avg = 25.36s`
- `cart_view_duration avg = 13.94s`
- `checkout_duration avg = 10.63s`
- `order_verify_duration avg = 4.26s`
- `http_req_duration p95 = 30.63s`
- `iteration_duration avg = 1m2s`

### 23.3 ماذا تعني هذه النتيجة؟

هذه النتيجة تعني أن النظام كان قادرًا على إكمال:

- `97` عملية checkout ناجحة من أصل `100`

وأن:

- `93` مستخدمًا أكملوا الرحلة كاملة حتى التحقق النهائي من الطلب

وهذه نسبة جيدة جدًا بالنسبة لرحلة شراء كاملة تحتوي على:

- authentication state
- cart updates
- database transaction
- row locking
- inventory updates
- order creation
- invoice dispatching

لكنها ليست `100%` بعد، وهو ما احتاج إلى تحليل إضافي.

---

## 24. محاولات التحسين أثناء اختبار `checkout`

### 24.1 تشغيل `login` داخل كل رحلة

تمت تجربة نسخة يكون فيها `login` جزءًا من الحمل المباشر على `100 VUs`.

#### النتيجة

النتيجة كانت ضعيفة جدًا مقارنة بالنسخة النهائية، حيث انخفض النجاح بشكل واضح، وظهرت مهلات وتأخيرات كبيرة.

#### السبب

اتضح أن:

- كل طلب محمي يحتاج المرور عبر JWT guard.
- JWT تستخدم cache/blacklist.
- هذه الطبقة كانت تعتمد على Redis البعيدة.

وبالتالي أصبح الاختناق في:

- authentication cache

وليس في `checkout` وحدها.

### 24.2 زيادة الـ sleep أكثر

تمت تجربة نسخة أخرى بـ sleeps أطول قليلًا مثل:

- `1.0`
- `2.0`
- `1.0`
- `2.0`
- `1.0`

لكن النتيجة لم تتحسن، بل انخفضت بعض نسب النجاح مقارنة بالضبط السابق.

#### الاستنتاج

ليست كل زيادة في `sleep` تؤدي بالضرورة إلى تحسن.  
الضبط الأفضل كان هو التوازن بين:

- واقعية السلوك
- وعدم إبطاء السيناريو بشكل مفرط

ولهذا تم اعتماد النسخة التي حققت:

- `checkout_ok = 97%`
- `flow_ok = 93%`

كأفضل نتيجة عملية.

---

## 25. تحليل أسباب الإخفاق المتبقي

بعد قراءة `laravel.log` أثناء هذه المرحلة، ظهرت مؤشرات واضحة جدًا على مصدر الاختناق.

### 25.1 Redis البعيدة كانت ما تزال نقطة اختناق

ظهر في السجل:

- `Predis\TimeoutException: Operation has timed out`
- `StreamInitException ... 192.168.18.18:6379`
- `Redis unavailable, switching to file cache`

وهذا يثبت أن Redis على:

`192.168.18.18:6379`

كانت تتأخر أو تفشل أحيانًا تحت الضغط.

### 25.2 JWT نفسها تعتمد على cache/blacklist

من الـ stack trace ظهر أن المسار يمر عبر:

- `tymon/jwt-auth`
- `Blacklist`
- `Illuminate Cache Repository`
- `RedisStore`

وهذا يعني أن مجرد التحقق من token في الطلبات المحمية كان يمر عبر Redis.

إذًا حتى بعد نقل `login` إلى `setup()`، بقيت الطلبات التالية متأثرة بالكاش البعيدة:

- `GET /cart`
- `POST /checkout`
- `GET /orders/{id}`

لأن المصادقة نفسها تفحص الـ token عبر طبقة مرتبطة بالكاش.

### 25.3 بعض التأخير لم يكن من منطق `checkout` فقط

لذلك يجب الانتباه إلى أن:

- ارتفاع `checkout_duration`
- وارتفاع `http_req_duration`

لا يعنيان دائمًا أن transaction الطلب نفسها هي السبب الوحيد، بل قد يكون السبب خليطًا من:

- JWT blacklist lookup
- Redis latency
- وقت الانتظار على الأقفال
- ضغط قاعدة البيانات

---

## 26. الاستنتاج النهائي من اختبار `checkout`

### 26.1 هل تم تنفيذ المتطلب؟

نعم، تم تنفيذ المتطلب بشكل صحيح وعملي، لأننا:

- أنشأنا سكربت مستقلًا لرحلة الشراء الكاملة.
- شغّلنا الاختبار على `100` مستخدم متزامن.
- جعلنا كل مستخدم ينفذ رحلة checkout كاملة.
- ضبطنا أزمنة `sleep` بما يعكس سلوكًا منطقيًا.
- حاولنا أكثر من ضبط للوصول إلى أفضل نتيجة.
- جمعنا نتائج فعلية قابلة للتوثيق.
- حللنا السجلات واكتشفنا مصدر الاختناقات.

### 26.2 ما أفضل نتيجة وصلنا إليها؟

أفضل نتيجة موثقة في هذه المرحلة كانت:

- `97%` نجاح مباشر لخطوة `checkout`
- `93%` نجاح للرحلة الكاملة من السلة حتى التحقق من الطلب

### 26.3 لماذا لم نصل إلى `100%`؟

السبب الأقرب والمدعوم بالسجلات هو:

- Redis البعيدة عبر الشبكة
- وتأثيرها على JWT blacklist/cache checks

أي أن الجزء المتبقي من الفشل يرتبط بالبيئة التشغيلية أكثر من كونه فشلًا خالصًا في منطق الشراء نفسه.

---

## 27. توصيات لتحسين النتيجة مستقبلًا

إذا أردنا تقريب النتيجة أكثر من `100%` في اختبارات `checkout` مستقبلًا، فالتوصيات هي:

1. نقل Redis إلى نفس الخادم المحلي بدل VM بعيدة.
2. تقليل الاعتماد على Redis في JWT blacklist أثناء الاختبار النهائي إذا كان ذلك ممكنًا.
3. تعطيل أي طبقة مراقبة إضافية غير ضرورية وقت الاختبار.
4. تنفيذ جولة إضافية بعد تنظيف البيئة وإعادة تشغيل الخدمات.
5. حفظ نتائج كل جولة checkout في ملف مستقل للمقارنة الرقمية المباشرة.

---

## 28. خلاصة خاصة باختبار `checkout`

اختبار `products` كان يختبر قدرة القراءة العامة والـ caching، أما اختبار `checkout` فقد كان أقرب بكثير إلى الواقع لأنه جمع بين:

- الكتابة على قاعدة البيانات
- الأقفال على السلة والمخزون
- إنشاء الطلبات
- خصم الكميات
- التحقق من نجاح الطلب

ولهذا فإن هذا الاختبار يمثل مستوى أعلى بكثير من التعقيد، والنتيجة التي تم الوصول إليها تعتبر قوية وذات قيمة أكاديمية واضحة، خصوصًا أن الاختبار كشف:

- أثر Redis البعيدة
- أثر المصادقة المعتمدة على cache
- الفرق بين نجاح endpoint بسيطة ونجاح رحلة شراء كاملة

---

## 29. اختبار الضغط على `register`

بعد توثيق اختبار `products` ثم اختبار `checkout`، تم تنفيذ اختبار ثالث مستقل على endpoint:

`POST /api/register`

والهدف من هذا الاختبار هو معرفة:

- هل يستطيع النظام تسجيل `100` مستخدم جديد تحت ضغط متزامن؟
- هل عملية إنشاء المستخدم وتشفير كلمة المرور وإصدار التوكن تعمل بثبات؟
- هل تظهر أخطاء وظيفية مثل:
  - `422`
  - `429`
  - `500`
- وهل الاختناق في هذه الحالة هو فشل منطقي، أم بطء زمني فقط؟

---

## 30. لماذا أنشأنا سكربت مستقل لـ `register`؟

تم إنشاء ملف جديد باسم:

`stress-register.js`

والسبب أن اختبار `register` يختلف عن `products` و`checkout` في عدة نقاط:

- هو endpoint عامة ولا تحتاج token مسبقًا.
- كل طلب يجب أن يحمل بريدًا إلكترونيًا فريدًا حتى لا يفشل التحقق بسبب `unique:users`.
- الاستجابة الناجحة لا تُرجع فقط بيانات المستخدم، بل تُرجع أيضًا `access_token`.

لذلك تم بناء سكربت مستقل يختبر:

1. نجاح إنشاء المستخدم.
2. نجاح إصدار التوكن.
3. زمن الاستجابة.

---

## 31. تصميم سيناريو اختبار `register`

السيناريو المعتمد كان مباشرًا وبسيطًا:

- `100` مستخدم متزامن `100 VUs`
- كل مستخدم ينفذ:
  - **طلب تسجيل واحد فقط**
- لا توجد خطوات إضافية مثل:
  - login
  - cart
  - checkout

هذا التصميم مناسب جدًا لأن الهدف هنا هو عزل عملية التسجيل نفسها وقياسها دون تشويش من أي خطوات أخرى.

### 31.1 البريد الإلكتروني الفريد

حتى لا تفشل الطلبات بسبب تكرار البريد الإلكتروني، تم توليد email مختلفة لكل مستخدم باستخدام:

- `runId`
- رقم المستخدم `__VU`
- رقم التكرار `__ITER`

وبذلك يصبح كل طلب `register` فريدًا من ناحية البيانات.

### 31.2 شكل البيانات المرسلة

كل مستخدم يرسل:

```json
{
  "name": "Register Stress User X",
  "email": "register_stress_<runId>_vu<vu>_iter<iter>@example.com",
  "password": "secret123",
  "password_confirmation": "secret123"
}
```

---

## 32. المقاييس التي تمت إضافتها في `stress-register.js`

تمت إضافة المقاييس التالية:

- `register_ok`
  - لقياس نجاح عملية التسجيل نفسها.
- `token_issued_ok`
  - لقياس نجاح إصدار التوكن بعد التسجيل.
- `register_duration`
  - لقياس زمن تنفيذ التسجيل.
- `status_201`
  - لعدّ الاستجابات الناجحة.
- `status_422`
  - لعدّ أخطاء التحقق من صحة البيانات.
- `status_429`
  - لعدّ أخطاء rate limiting.
- `status_500`
  - لعدّ أخطاء السيرفر الداخلية.

كما تم تعريف thresholds كالتالي:

- `http_req_failed < 5%`
- `http_req_duration p95 < 5000ms`
- `register_ok > 95%`
- `token_issued_ok > 95%`

---

## 33. التحقق الصغير قبل الاختبار الكامل

قبل تشغيل جولة `100` مستخدم، تم تشغيل تحقق صغير على:

- `5` مستخدمين متزامنين

والهدف كان التأكد من:

- صحة endpoint
- صحة صيغة الطلب
- صحة توليد email الفريدة
- عودة `201`
- عودة `access_token`

### نتيجة التحقق الصغير

النتيجة كانت ناجحة بالكامل:

- `register_ok = 100%`
- `token_issued_ok = 100%`
- `p95 ≈ 1.51s`

وهذا أكد أن السكربت صحيح قبل إطلاق الجولة الكبيرة.

---

## 34. أمر تشغيل الاختبار الكامل

تم تشغيل الاختبار الفعلي باستخدام:

```powershell
& C:\Progra~1\k6\k6.exe run `
  -e BASE_URL=http://ecommerce.local/api `
  -e TARGET_USERS=100 `
  -e MAX_DURATION=10m `
  stress-register.js
```

وكان الرابط الأساسي:

`http://ecommerce.local/api`

أي من خلال `Apache Virtual Host`.

---

## 35. النتائج الفعلية لاختبار `register`

في الجولة الأساسية على `100` مستخدم متزامن ظهرت النتائج التالية:

### 35.1 نتائج النجاح

- `register_ok = 100.00%`
- `token_issued_ok = 100.00%`
- `status_201 = 100`
- `checks_succeeded = 100.00%`
- `http_req_failed = 0.00%`

### 35.2 نتائج الزمن

- `register_duration avg = 21.56s`
- `register_duration med = 22.28s`
- `register_duration p90 = 26.32s`
- `register_duration p95 = 26.73s`
- `register_duration max = 27.05s`

### 35.3 نتائج التنفيذ

- `iterations = 100`
- `vus_max = 100`
- `100 complete and 0 interrupted iterations`

---

## 36. ماذا تعني هذه النتيجة؟

هذه النتيجة مهمة جدًا لأنها تكشف نقطة دقيقة:

### 36.1 من الناحية الوظيفية

الاختبار كان ناجحًا بالكامل تقريبًا من الناحية الوظيفية:

- كل المستخدمين `100/100` تم تسجيلهم بنجاح.
- كل المستخدمين حصلوا على `access_token`.
- لم تظهر:
  - `422`
  - `429`
  - `500`
- لم يظهر فشل شبكي عام في الطلبات.

أي أن endpoint `register` كانت **مستقرة وظيفيًا** تحت الضغط.

### 36.2 من ناحية الأداء الزمني

رغم النجاح الوظيفي الكامل، فإن الأداء الزمني كان ضعيفًا:

- `p95 = 26.73s`

بينما الشرط المحدد في threshold كان:

- `p95 < 5s`

وهذا يعني أن endpoint لم تنهَر، لكنها أصبحت بطيئة جدًا عند تنفيذ `100` عمليات تسجيل متزامنة.

---

## 37. لماذا كانت `register` بطيئة رغم أنها لم تفشل؟

أغلب الظن أن البطء في هذه الحالة ناتج عن مزيج من:

1. **تشفير كلمات المرور**
   - `Hash::make()` عملية مكلفة نسبيًا، وعند تنفيذها `100` مرة بالتوازي يظهر أثرها بوضوح.
2. **إنشاء سجل المستخدم في قاعدة البيانات**
   - كل تسجيل يضيف صفًا جديدًا فعليًا إلى جدول `users`.
3. **إصدار JWT token**
   - بعد إنشاء المستخدم، يتم استدعاء:
   - `JWTAuth::fromUser($user)`
4. **ضغط التطبيق المحلي نفسه**
   - الاختبار نُفذ على بيئة محلية وليس على خادم إنتاجي قوي.

بمعنى آخر:

- `register` لم تظهر كمسار غير مستقر.
- لكنها ظهرت كمسار **مكلف حسابيًا** تحت التزامن العالي.

---

## 38. مقارنة سريعة بين `register` و `checkout`

الفرق بين النتيجتين كان واضحًا:

### `register`

- **نجاح وظيفي كامل تقريبًا**
- **بطء زمني مرتفع**
- لم تظهر أخطاء منطقية كثيرة

### `checkout`

- **نجاح وظيفي مرتفع لكن ليس كاملًا**
- **بطء زمني مرتفع**
- ظهرت أخطاء `400` و `500`
- ظهر أثر Redis والـ JWT blacklist بوضوح

إذًا:

- `register` أبرزت مشكلة **الأداء الزمني**
- `checkout` أبرزت مشكلة **الأداء الزمني + بعض الإخفاق الوظيفي**

---

## 39. الحكم النهائي على اختبار `register`

يمكن تلخيص نتيجة اختبار `register` كالتالي:

- endpoint `register` **نجحت وظيفيًا بنسبة 100%** في هذا الاختبار.
- جميع المستخدمين تم إنشاؤهم بنجاح.
- جميع المستخدمين حصلوا على token.
- لم تظهر أخطاء تحقق أو rate limit أو أخطاء داخلية.
- لكن زمن الاستجابة تحت `100` مستخدم متزامن كان مرتفعًا جدًا.

وهذا يعني أن:

- المسار **stable but slow under concurrency**

أي:

- مستقر من حيث الوظيفة
- لكنه بطيء من حيث الأداء

---

## 40. ماذا أضاف اختبار `register` إلى تقرير المتطلب التاسع؟

إضافة هذا الاختبار مهمة لأنها تكمل صورة المشروع تحت الضغط:

- `products`
  - تمثل قراءة عامة مع caching
- `checkout`
  - تمثل رحلة تجارية مع transaction وlocking
- `register`
  - تمثل كتابة مكثفة + hashing + token generation

وبذلك أصبح تقرير المتطلب التاسع لا يغطي نوعًا واحدًا من الضغط، بل يغطي:

- مسار قراءة
- مسار شراء معقّد
- مسار إنشاء حسابات جديدة

وهذا يجعل التقرير أقوى أكاديميًا وأكثر شمولًا.
