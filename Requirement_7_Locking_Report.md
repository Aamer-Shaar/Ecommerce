# تقرير المتطلب السابع: التحكم في الأقفال

## 1. مقدمة

هذا التقرير يشرح كيفية تحقيق المتطلب السابع في مشروع المتجر الإلكتروني، وهو:

**التحكم في الأقفال (Locking Control)**

الهدف من هذا المتطلب هو حماية البيانات الحساسة من التعارض عند وصول أكثر من طلب في نفس الوقت إلى نفس المورد، خاصة في العمليات التي قد تؤدي إلى فقدان الاتساق أو إنتاج نتائج غير صحيحة مثل:

- خصم المخزون.
- تنفيذ عملية شراء متزامنة.
- تعديل السلة أثناء إنشاء الطلب.

في هذا المشروع تم تطبيق قفلين رئيسيين داخل مسار إنشاء الطلب `checkout`:

1. قفل على **صفوف السلة الخاصة بالمستخدم**.
2. قفل على **صفوف المخزون الخاصة بالمنتجات المطلوبة**.

---

## 2. ما نوع الأقفال المستخدمة؟

القفلان المستخدمان في المشروع هما من نوع:

**Pessimistic Locking**

وليسا من نوع:

**Optimistic Locking**

والسبب في ذلك أن الكود يستخدم الدالة:

```php
lockForUpdate()
```

وهذه الدالة تطلب من قاعدة البيانات حجز الصف فعليًا داخل المعاملة `transaction` حتى تنتهي العملية، وبالتالي فإن أي طلب آخر يحاول تعديل نفس الصف يجب أن ينتظر.

هذا هو السلوك المعروف باسم:

**Pessimistic Locking**

أما `Optimistic Locking` فعادةً يحتاج إلى:

- عمود إضافي مثل `version`.
- أو مقارنة قيمة `updated_at`.
- أو تنفيذ تحديث مشروط مثل: التحديث فقط إذا كانت النسخة الحالية لم تتغير.

وهذا النمط غير موجود حاليًا في المشروع.

---

## 3. القفل الأول: قفل السلة

### 3.1 مكان القفل

تم تطبيق قفل السلة داخل:

`app/Http/Controllers/Api/OrderController.php`

وتحديدًا داخل الدالة:

`checkout()`

حيث يبدأ الكود المعاملة أولًا، ثم يقرأ صفوف السلة الخاصة بالمستخدم مع:

```php
$cartItems = Cart::with('product')
    ->where('user_id', $user->id)
    ->lockForUpdate()
    ->get();
```

### 3.2 نوع هذا القفل

هذا القفل من نوع:

**Pessimistic Locking**

لأن صفوف السلة تُقفل فعليًا أثناء تنفيذ عملية الشراء، فلا يستطيع طلب آخر تعديل نفس صفوف السلة في الوقت نفسه حتى تنتهي المعاملة.

### 3.3 لماذا استخدمنا قفل السلة؟

تم استخدام هذا القفل لحماية عملية `checkout` من التعارضات المتزامنة، مثل:

- قيام المستخدم بإرسال طلبي `checkout` في اللحظة نفسها.
- تعديل السلة من نافذة أخرى أو جهاز آخر أثناء إنشاء الطلب.
- حذف عنصر من السلة بينما لا تزال عملية إنشاء الطلب تقرأ نفس البيانات.

لو لم يكن هذا القفل موجودًا، فقد يقرأ النظام السلة في حالة، ثم تتغير السلة أثناء التنفيذ، وهذا قد يؤدي إلى:

- إنشاء طلب غير مطابق للحالة النهائية للسلة.
- تكرار عملية الشراء.
- سلوك غير متوقع عند التزامن العالي.

### 3.4 كيف يعمل هذا القفل عمليًا؟

عندما يبدأ أول طلب `checkout`:

1. يبدأ `transaction`.
2. يتم قفل صفوف السلة الخاصة بالمستخدم بواسطة `lockForUpdate()`.
3. يبقى القفل قائمًا حتى `commit` أو `rollback`.

إذا وصل طلب ثانٍ لنفس المستخدم في الوقت نفسه، فسيضطر إلى الانتظار حتى ينتهي الطلب الأول، وبذلك نضمن أن بناء الطلب يتم على نسخة ثابتة ومتسقة من السلة.

### 3.5 لماذا لم نستخدم Optimistic Locking هنا؟

لأن عملية الشراء عملية حساسة ولا نريد فيها فقط اكتشاف التعارض بعد وقوعه، بل نريد منعه قبل أن يسبب مشكلة.

في حالة `checkout`، الأفضل أن ينتظر الطلب الثاني بدل أن نحاول لاحقًا اكتشاف أن السلة تغيّرت أثناء التنفيذ.

لذلك كان `Pessimistic Locking` هو الأنسب.

---

## 4. القفل الثاني: قفل المخزون

### 4.1 مكان القفل

تم تطبيق قفل المخزون أيضًا داخل:

`app/Http/Controllers/Api/OrderController.php`

داخل الحلقة التي تمر على عناصر السلة:

```php
$inventory = \App\Models\Inventory::where('product_id', $item->product_id)
    ->lockForUpdate()
    ->first();
```

### 4.2 نوع هذا القفل

هذا القفل أيضًا من نوع:

**Pessimistic Locking**

لأن صف المخزون الخاص بالمنتج يتم قفله قبل فحص الكمية وقبل الخصم منها.

### 4.3 لماذا استخدمنا قفل المخزون؟

هذا هو أهم قفل في النظام لأنه يمنع مشكلة:

**Overselling**

أي بيع كمية أكبر من الكمية المتوفرة فعليًا في المخزون.

مثال على المشكلة بدون قفل:

- الكمية المتوفرة لمنتج ما = 5
- مستخدم أول يريد شراء 4
- مستخدم ثانٍ يريد شراء 3 في اللحظة نفسها

إذا قرأ الطلبان الكمية نفسها في الوقت نفسه بدون قفل، فقد يعتبر كل واحد منهما أن الكمية كافية، ثم يتم الخصم مرتين، فتظهر نتيجة غير صحيحة أو سالبة أو يحدث بيع فوق المتاح.

أما مع `lockForUpdate()`:

- الطلب الأول يقفل صف المخزون.
- الطلب الثاني ينتظر.
- بعد انتهاء الأول، يقرأ الثاني القيمة الجديدة الحقيقية.
- إذا لم تعد الكمية كافية، يتم رفض العملية.

### 4.4 كيف يعمل هذا القفل في الكود؟

التسلسل داخل `checkout` هو:

1. قراءة عنصر من السلة.
2. الوصول إلى سجل المخزون الموافق له.
3. قفل سجل المخزون بـ `lockForUpdate()`.
4. التحقق من أن الكمية كافية.
5. تنفيذ الخصم:

```php
$inventory->decrement('quantity', $item->quantity);
```

6. الاستمرار إلى المنتج التالي.

وبما أن كل هذا يتم داخل نفس المعاملة، فإن الاتساق يبقى محفوظًا حتى نهاية العملية.

### 4.5 لماذا لم نستخدم Optimistic Locking هنا؟

في المخزون، التعارض متوقع جدًا عند الشراء المتزامن، لذلك من الأفضل منع التعارض فورًا بدل الاكتفاء باكتشافه لاحقًا.

كما أن فشل تحديث المخزون بعد اكتشاف تعارض optimistic قد يفرض إعادة المحاولة أو إلغاء العملية، وهذا أكثر تعقيدًا منطقًا وتنفيذًا في مسار الشراء.

لذلك كان القفل التشاؤمي هو الحل الأكثر أمانًا ووضوحًا هنا.

---

## 5. لماذا القفلان من نفس النوع؟

قد يبدو أن المتطلب يطلب أكثر من قفل، وليس بالضرورة أكثر من **نوع** قفل.

في هذا المشروع تم اختيار تطبيق قفلين مختلفين على موردين مختلفين، لكن باستخدام **نفس النوع** وهو `Pessimistic Locking`، لأن كلا الموردين يدخلان في مسار حساس جدًا:

- السلة تمثل البيانات التي سيُبنى عليها الطلب.
- المخزون يمثل المورد الذي يجب حمايته من التناقص غير الصحيح.

إذن التنوع هنا ليس في نوع القفل، بل في **مكان التطبيق والغرض منه**:

- قفل السلة يحمي **اتساق بيانات الطلب**.
- قفل المخزون يحمي **سلامة الكميات ومنع البيع الزائد**.

---

## 6. شرح منطق التنفيذ داخل checkout

الخطوات الحالية في الكود أصبحت كالتالي:

1. يبدأ النظام معاملة قاعدة بيانات `DB::beginTransaction()`.
2. يتم قفل صفوف السلة الخاصة بالمستخدم.
3. يتم التأكد من أن السلة ليست فارغة.
4. يمر النظام على كل عنصر في السلة.
5. لكل عنصر، يتم قفل صف المخزون الموافق له.
6. يتم التحقق من الكمية المتاحة.
7. يتم خصم الكمية من المخزون.
8. بعد اكتمال الفحص والخصم، يتم إنشاء سجل الطلب.
9. يتم إنشاء عناصر الطلب `OrderItem`.
10. يتم حذف عناصر السلة.
11. يتم تنفيذ `commit`.
12. بعد نجاح العملية يتم مسح الكاشات المتعلقة بالمنتجات والمخزون.

هذا التسلسل يضمن أن العملية تتم بشكل ذري وآمن نسبيًا من التعارضات.

---

## 7. ماذا كان يمكن أن يحدث بدون هذه الأقفال؟

### بدون قفل السلة

قد يحدث:

- تنفيذ طلبين على نفس السلة.
- تعديل السلة أثناء إنشاء الطلب.
- عدم تطابق بين محتوى الطلب النهائي وما كان موجودًا عند بداية التنفيذ.

### بدون قفل المخزون

قد يحدث:

- خصم متزامن غير صحيح.
- بيع كمية أكبر من المتوفر.
- فشل في اتساق بيانات المخزون.

---

## 8. هل يوجد Optimistic Locking في المشروع؟

لا، لا يوجد حاليًا تطبيق فعلي لـ `Optimistic Locking` في المشروع.

ولو أردنا تطبيقه مستقبلًا، فسنحتاج إلى تعديل بنية الجداول أو منطق التحديث، مثل:

- إضافة عمود `version`.
- قراءة النسخة الحالية للسجل.
- تنفيذ التحديث بشرط أن النسخة لم تتغير.
- إذا تغيرت، يتم إلغاء العملية أو إعادة المحاولة.

لكن هذا لم يكن الخيار الأفضل هنا لأن مسار الشراء والمخزون يحتاج إلى حماية مباشرة وفورية من التعارض.

---

## 9. الخلاصة

تم تحقيق المتطلب السابع في المشروع عبر تطبيق قفلين فعليين داخل عملية الشراء:

1. **قفل السلة**
   - المكان: `checkout`
   - النوع: `Pessimistic Locking`
   - الهدف: منع تعديل السلة أو تنفيذ شراء متزامن على نفس السلة أثناء إنشاء الطلب

2. **قفل المخزون**
   - المكان: `checkout`
   - النوع: `Pessimistic Locking`
   - الهدف: منع التعارض على الكميات وحماية النظام من `overselling`

وبذلك أصبح النظام أكثر اتساقًا عند التزامن، خاصة في أهم عملية تجارية في المشروع وهي إنشاء الطلب وخصم المخزون.
